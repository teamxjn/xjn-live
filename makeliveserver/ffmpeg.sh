# Ubuntu/Debian
sudo apt update
sudo apt install -y ffmpeg

# macOS (Homebrew 사용)
brew install ffmpeg

# Windows
# 1. https://ffmpeg.org/download.html에서 다운로드
# 2. 압축 해제 후 환경 변수에 추가

# 설치 확인
ffmpeg -version
